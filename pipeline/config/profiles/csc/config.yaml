# Snakemake 8.x Profile for CSC Puhti - SLURM + Apptainer
# Usage: snakemake --profile config/profiles/csc --config bids_root=... derivatives_root=... container_image=...

# SLURM executor
executor: slurm

# Job settings
jobs: 100
keep-going: true
latency-wait: 120
retries: 1
printshellcmds: true
rerun-incomplete: true
local-cores: 1

# Default SLURM resources
default-resources:
  - slurm_account=project_2001988
  - slurm_partition=small
  - mem_mb=8000
  - runtime=120

# Apptainer container support
software-deployment-method:
  - apptainer

# CSC already binds /scratch and /projappl by default
# Only set HSF cache location
apptainer-args: "--env HSF_HOME=/users/$USER/.hsf"