[project]
name = "neuroimaging-pipeline"
version = "0.1.0"
description = "MRI data processing pipeline"
requires-python = ">=3.8"

dependencies = [
    "snakemake>=7.0.0",
    "pandas>=1.5.0",
    "openpyxl>=3.0.0",
]